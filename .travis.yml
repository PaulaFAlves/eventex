language: python
python:
  - "3.7.4"
install:
  - cp contrib/env-sample .env
  - pip install -q -r requirements.txt
  - pip install codecov 
script:
  - coverage run tests.py
  - python manage.py test
